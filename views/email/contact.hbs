<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Arial, sans-serif; color: #171717; }
    .container { background: #ffffff; padding: 20px; border-radius: 8px; }
    .header { font-size: 1.25rem; margin-bottom: 10px; }
    .message {font-size: 1rem  white-space: pre-wrap; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">Neue Kontaktanfrage 📬</div>
    <p><strong>Name:</strong> {{name}}</p>
    <p><strong>E-Mail:</strong> {{email}}</p>
    <div class="message">{{message}}</div>
  </div>
</body>
</html>